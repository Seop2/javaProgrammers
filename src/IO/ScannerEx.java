package IO;

import java.util.Scanner;

public class ScannerEx {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int n = sc.nextInt();
        long l = sc.nextLong();
        String s = sc.next();
        String s2 = sc.nextLine();
        /**
         * nextLine()은 다른 메소드들과 사용하면 문제가 생겨, nextLine()을 쓸 경우 nextLine()만 사용
         */
    }
}
